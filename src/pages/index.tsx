import React, {FC} from 'react';
import {Header} from "@/components";

const Home: FC = () => {
    return (
        <div>
            <Header/>
            <label>Home</label>
        </div>
    );
};

export default Home;
